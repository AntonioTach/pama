---
import Logo from './Logo.astro';

export interface Props {
  title?: string;
  subtitle?: string;
  ctaText?: string;
  ctaLink?: string;
}

const { 
  title = "PAMA", 
  subtitle = "Decoración con globos para eventos inolvidables",
  ctaText = "Contáctanos",
  ctaLink = "#contact"
} = Astro.props;
---

<section id="hero" class="w-full min-h-[85vh] flex flex-col justify-center items-center bg-gradient-to-br from-pink-100 via-blue-50 to-yellow-100 relative overflow-hidden">
  <!-- Partículas flotantes (reducidas) -->
  <div class="absolute inset-0 pointer-events-none select-none z-0">
    {Array.from({ length: 8 }).map((_, i) => (
      <div 
        class="particle" 
        style={`left: ${Math.random() * 100}%; animation-delay: ${Math.random() * 8}s; animation-duration: ${6 + Math.random() * 4}s; opacity: 0.4;`}
      ></div>
    ))}
  </div>

  <!-- Globos animados SVG (más sutiles) -->
  <div class="absolute inset-0 pointer-events-none select-none z-0">
    <svg class="absolute left-10 top-20 animate-float-slow" width="70" height="140" viewBox="0 0 70 140" fill="none" xmlns="http://www.w3.org/2000/svg">
      <ellipse cx="35" cy="60" rx="32" ry="50" fill="#F472B6" fill-opacity="0.6"/>
      <rect x="32" y="110" width="6" height="30" rx="3" fill="#F472B6"/>
      <circle cx="30" cy="55" r="2" fill="white" fill-opacity="0.5"/>
    </svg>
    
    <svg class="absolute right-20 top-32 animate-float" width="50" height="100" viewBox="0 0 50 100" fill="none" xmlns="http://www.w3.org/2000/svg">
      <ellipse cx="25" cy="45" rx="22" ry="40" fill="#60A5FA" fill-opacity="0.6"/>
      <rect x="22" y="85" width="6" height="15" rx="3" fill="#60A5FA"/>
      <circle cx="20" cy="40" r="1.5" fill="white" fill-opacity="0.5"/>
    </svg>
    
    <svg class="absolute left-1/2 bottom-10 animate-float-slow" style="transform:translateX(-50%)" width="80" height="160" viewBox="0 0 80 160" fill="none" xmlns="http://www.w3.org/2000/svg">
      <ellipse cx="40" cy="70" rx="38" ry="60" fill="#FDE68A" fill-opacity="0.6"/>
      <rect x="36" y="130" width="6" height="30" rx="3" fill="#FDE68A"/>
      <circle cx="35" cy="65" r="3" fill="white" fill-opacity="0.5"/>
    </svg>

    <svg class="absolute left-1/4 top-1/2 animate-float-delayed-1" width="40" height="80" viewBox="0 0 40 80" fill="none" xmlns="http://www.w3.org/2000/svg">
      <ellipse cx="20" cy="35" rx="18" ry="30" fill="#34D399" fill-opacity="0.5"/>
      <rect x="17" y="65" width="5" height="15" rx="2.5" fill="#34D399"/>
    </svg>

    <svg class="absolute right-1/4 bottom-1/3 animate-float-delayed-2" width="60" height="120" viewBox="0 0 60 120" fill="none" xmlns="http://www.w3.org/2000/svg">
      <ellipse cx="30" cy="50" rx="27" ry="45" fill="#A78BFA" fill-opacity="0.5"/>
      <rect x="26" y="95" width="6" height="25" rx="3" fill="#A78BFA"/>
    </svg>
  </div>

  <!-- Elementos decorativos (más sutiles) -->
  <div class="absolute top-16 left-16 animate-bounce-gentle text-3xl opacity-60">✨</div>
  <div class="absolute bottom-16 right-16 animate-float-slow text-3xl opacity-60">💫</div>
  
  <!-- Logo -->
  <div class="mt-20 z-10 animate-fade-in-up">
    <Logo size="xl" className="mb-6" />
  </div>
  
  <h1 class="text-5xl md:text-7xl font-extrabold text-pink-600 drop-shadow-lg text-center z-10 animate-fade-in-up" style="animation-delay: 0.2s">
    {title}
  </h1>
  
  <p class="text-2xl md:text-3xl text-blue-700 mt-6 mb-12 text-center font-semibold z-10 animate-fade-in-up" style="animation-delay: 0.3s">
    {subtitle}
  </p>
  
  <a 
    href={ctaLink} 
    class="px-10 py-4 bg-gradient-to-r from-pink-400 to-purple-400 text-white rounded-full shadow-xl text-xl font-bold z-10 hover:scale-105 hover:shadow-2xl transition-all duration-300 focus:outline-none focus:ring-4 focus:ring-pink-300 focus:ring-offset-2" 
    tabindex="0" 
    aria-label="Ir a contacto"
  >
    {ctaText}
  </a>

  <!-- Flecha de scroll (más sutil) -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce-gentle z-10 opacity-70">
    <div class="text-gray-600 text-xl">↓</div>
  </div>
</section> 